<Page x:Class="DuneEdit.SietchDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="SietchDetailsPage">
    <Grid Name="detailsPanelData">
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF100B0B" Offset="0" />
                <GradientStop Color="#FF583434" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="10" /> <!-- Margin -->
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <SolidColorBrush x:Key="DetailsTitleBrush" Color="#EBC700" />
            <SolidColorBrush x:Key="DetailsWaterBrush" Color="#59AAFF" />
            <SolidColorBrush x:Key="DetailsValueBrush" Color="#BEBEBE" />

            <Style x:Key="DetailsTitle"            TargetType="Label">
                <Setter Property="Grid.Column"     Value="0" />
                <Setter Property="Grid.ColumnSpan" Value="3" />
                <Setter Property="Foreground"      Value="{StaticResource DetailsTitleBrush}" />
                <Setter Property="FontWeight"      Value="Bold" />
            </Style>
            <Style x:Key="DetailsSubTitle"         TargetType="Label">
                <Setter Property="Grid.Column"     Value="0" />
                <Setter Property="Grid.ColumnSpan" Value="3" />
                <Setter Property="Foreground"      Value="{StaticResource DetailsTitleBrush}" />
                <Setter Property="FontStyle"       Value="Italic" />
            </Style>
            <Style x:Key="DetailsLocName"          TargetType="Label">
                <Setter Property="Grid.Column"     Value="0" />
                <Setter Property="Grid.ColumnSpan" Value="3" />
                <Setter Property="Foreground"      Value="{StaticResource DetailsWaterBrush}" />
            </Style>
            <Style x:Key="DetailsValueName"        TargetType="Label">
                <Setter Property="Grid.Column"     Value="0" />
                <Setter Property="Foreground"      Value="{StaticResource DetailsValueBrush}" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>
            <Style x:Key="DetailsValueText"        TargetType="TextBox">
                <Setter Property="Grid.Column"     Value="2" />
                <Setter Property="Width"           Value="40" />
                <Setter Property="Margin"          Value="8" />
                <Setter Property="Background"      Value="#E0E0E0" />
            </Style>
            <Style x:Key="DetailsValueCheck"       TargetType="CheckBox">
                <Setter Property="Grid.Column"     Value="2" />
                <Setter Property="Width"           Value="40" />
                <Setter Property="Margin"          Value="8" />
                <!--->
                <Setter Property="Template"        Value="{StaticResource ThemedCheckbox}" /> -->
                <Setter Property="SnapsToDevicePixels" Value="true"/>
                <Setter Property="OverridesDefaultStyle" Value="true"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <BulletDecorator Background="Transparent">
                                <BulletDecorator.Bullet>
                                    <Border x:Name="Border" 
                                                        Width="14" 
                                                        Height="14" 
                                                        CornerRadius="2" 
                                                        Background="#E0E0E0"
                                                        BorderThickness="1"
                                                        BorderBrush="#404040">
                                        <Path 
                                                        Width="8" Height="8" 
                                                        x:Name="CheckMark"
                                                        SnapsToDevicePixels="False"
                                                        StrokeThickness="2"
                                                        Stroke="{StaticResource DetailsTitleBrush}"
                                                        Data="M 0 4 L 3 8 8 0" />
                                    </Border>
                                </BulletDecorator.Bullet>
                                <ContentPresenter Margin="4,0,0,0"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Left"
                                                        RecognizesAccessKey="True"/>
                            </BulletDecorator>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="false">
                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="{x:Null}">
                                    <Setter TargetName="CheckMark" Property="Data" Value="M 0 7 L 7 0" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter TargetName="Border" Property="Background" Value="#808080" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="true">
                                    <Setter TargetName="Border" Property="Background" Value="#E0E0E0" />
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#606060" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="false">
                                    <Setter TargetName="Border" Property="Background" Value="#EEEEEE" />
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#AAAAAA" />
                                    <Setter Property="Foreground" Value="#888888"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Label Grid.Row="0" Content="{Binding LocationTypeTitle}" Style="{StaticResource DetailsTitle}" />
        <Label Grid.Row="1" Content="{Binding Path=Name}" Style="{StaticResource DetailsLocName}" />
    </Grid>
</Page>
